1. install node (make sure python has been installed in your PC)
2. run 'npm install' at root directory (if npm start not work, run this)
3. run 'npm start' at root directory
4. open 'http://localhost:8000/' using browser

/**
if errors show, run 'webpack app.js dist/bundle.js' after step 2.
**/
